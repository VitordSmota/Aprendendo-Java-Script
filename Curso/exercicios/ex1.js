// Crie uma função que dado dois valores (passados como parâmetros)
//mostre no console a soma, subtração, multiplicação e divisão desses valores


const soma = (a, b) => a + b

const sub = (a, b) => a - b

const mul = (a, b) => a * b

const div = (a, b) => a / b

console.log(` Soma de 1 + 1: ${soma(1,1)}`);
console.log(` Sub de 5 - 7: ${sub(5,7)}`);
console.log(` Multi de 5 x 5: ${mul(5,5)}`);
console.log(` Div de 25 por 5: ${div(25,5)}`);
